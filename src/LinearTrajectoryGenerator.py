
class LinearTrajectoryGenerator():

    def __init__(self):
        self.path = []
        self.trajectory = []
        self.MAX_V = None
        self.MAX_A = None
    def build_trajectory(self):
        for i in range(len(path)):
            # if turn turn first then move linear move 
            # split x and y componenets 
            
            # Accerlation

            # Decerlation 
            # Coasting 
            if i > 0:

            else:
                v_cruise = min(self.MAX_V,) # WIP looking at Cheesy poofs(254) presentation on trapzoidal motion profiles

    def getTrajectory(self):
        return self.trajectory
